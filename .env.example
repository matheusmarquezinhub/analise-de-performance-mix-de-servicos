# Keep real secrets only in .env (already ignored by git).

DB_DRIVER=ODBC Driver 17 for SQL Server
DB_SERVER=SEU_SERVIDOR_SQL
DB_NAME=SEU_BANCO_DE_DADOS
DB_TRUSTED_CONNECTION=yes
DB_TRUST_SERVER_CERT=yes
